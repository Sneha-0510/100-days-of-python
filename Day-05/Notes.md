# For Loops in List
Loops allow us to tell the computer to repeat actions without having to write repeated code.
Loops allow us to create a rule and the computer can follow it to do a repeated action.

### Syntax

```
for <variable name of each item> in <a List>:
    <do something>
    <do something else> 
```

```
 example :
fruits = ["apple" , "mango , "banana"]
for i in fruits:
print(fruits + "pie")
```

## Conclusion

Day 05 helped me strengthen my understanding of **loops and lists in Python**. I learned how to use `for` loops to iterate through lists, perform operations on individual elements, and find the highest value in a list. I also learned how to use `range()` with `for` loops to control the number of iterations and generate sequences of numbers.

Finally, I applied these concepts in a **Password Generator project**, where I combined loops, lists, the `random` module, and string manipulation to create a simple and functional program.

Overall, this day improved my ability to think logically, repeat tasks efficiently using loops, and apply multiple Python concepts together to build a small real-world project.
